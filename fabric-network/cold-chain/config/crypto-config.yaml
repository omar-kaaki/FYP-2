# Crypto configuration for Cold Chain (Archived Evidence)
# Defines the PKI structure for ForensicLabMSP, CourtMSP, and OrdererOrg

OrdererOrgs:
  - Name: OrdererOrg
    Domain: orderer.cold.dfir.local
    EnableNodeOUs: true

    Specs:
      - Hostname: orderer0
      - Hostname: orderer1
      - Hostname: orderer2

    # TLS Configuration
    CA:
      Hostname: ca-orderer

PeerOrgs:
  # Forensic Lab Organization
  - Name: ForensicLabMSP
    Domain: lab.cold.dfir.local
    EnableNodeOUs: true

    # Lab peers
    Template:
      Count: 2
      Start: 0
      Hostname: peer
      SANS:
        - localhost
        - 127.0.0.1

    # Lab users
    Users:
      Count: 3

    CA:
      Hostname: ca-lab

  # Court Organization
  - Name: CourtMSP
    Domain: court.cold.dfir.local
    EnableNodeOUs: true

    # Court peers
    Template:
      Count: 2
      Start: 0
      Hostname: peer
      SANS:
        - localhost
        - 127.0.0.1

    # Court users
    Users:
      Count: 3

    CA:
      Hostname: ca-court
